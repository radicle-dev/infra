{
    "storage-driver": "zfs",
    "storage-opts": [
        "size=2.5GB",
        "zfs.fsname=${__DOCKER_ZFS_DATASET}"
    ],
    "runtimes": {
        "kata-runtime": {
            "path": "/usr/bin/kata-runtime"
        }
    }
}
